{
  "openapi" : "3.0.1",
  "info" : {
    "description" : "Better Metadata - Better Science\n\nThe monitor server reports on the rest of CEDAR: how deep the work queues are, how much each\nstore holds, whether a given server considers itself healthy, and what artifact a given string\nrefers to.\n\nEverything here is read-only and needs the monitor read permission. The counts are meant to be\nread against each other: the workspace graph is authoritative, and the search index disagreeing\nwith it is how a half-finished reindex shows itself.\n",
    "title" : "CEDAR Monitor Server API",
    "version" : "2.9.7-SNAPSHOT"
  },
  "paths" : {
    "/command/resource-id-lookup" : {
      "get" : {
        "description" : "Take anything that might name a CEDAR artifact — an identifier, an IRI, a URL from the workbench, a path with a query string — and report the artifact identifier it resolves to, and how that was arrived at. Written for reading a support ticket or a log line, where what arrives is whatever a user pasted. Always answers 200: a string that resolves to nothing is a finding, not a failed request, and the body says so.",
        "operationId" : "lookUpResource",
        "parameters" : [ {
          "description" : "The string to resolve. Leading and trailing space is trimmed; the sanitized form is echoed in the response.",
          "in" : "query",
          "name" : "input",
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What the input resolved to, and how"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Work out which artifact a string refers to",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/health-check/{server}" : {
      "get" : {
        "description" : "Proxy the named server's health check. The status and body are the other server's own, so a 500 here can mean that server is unhealthy rather than that this one failed.",
        "operationId" : "healthCheck",
        "parameters" : [ {
          "description" : "CEDAR server name, as the configuration spells it. Examples: artifact, terminology, resource.",
          "in" : "path",
          "name" : "server",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "The named server's health check, as it reported it"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "404" : {
            "description" : "No CEDAR server answers to this name"
          },
          "500" : {
            "description" : "The named server reported itself unhealthy, or its response could not be read"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get another server's health check",
        "tags" : [ "Health checks" ]
      }
    },
    "/mysql/counts" : {
      "get" : {
        "description" : "Report every table in every MySQL schema CEDAR's configured datasources can see — the application log database and the messaging database — with each table's row count, engine, and the space its data, indexes and freed pages occupy. Row counts are the optimizer's estimate unless exact=true is asked for, which counts the rows instead and can take a while over the log tables.",
        "operationId" : "mySqlCounts",
        "parameters" : [ {
          "description" : "Count rows with COUNT(*) instead of reading the optimizer's estimate",
          "in" : "query",
          "name" : "exact",
          "schema" : {
            "type" : "boolean",
            "default" : false
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "A per-table report for each reachable database"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Count and measure what MySQL holds",
        "tags" : [ "Counts" ]
      }
    },
    "/redis/queue-counts" : {
      "get" : {
        "description" : "Report how many items are waiting in each of CEDAR's Redis queues: search permissions, NCBI submissions, application logs, value recommender, and instance cloning. A queue that keeps growing is the sign that whatever drains it has stopped.",
        "operationId" : "queueCounts",
        "responses" : {
          "200" : {
            "description" : "A count per queue"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get the depth of each work queue",
        "tags" : [ "Queues" ]
      }
    },
    "/resource/folders" : {
      "get" : {
        "description" : "Gather what each store holds about one folder into a single answer: the workspace graph's record of it and its path, its computed permissions, and the OpenSearch document. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a folder that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getFolderInfo",
        "parameters" : [ {
          "description" : "Identifier of the folder to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the folder"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a folder",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/groups" : {
      "get" : {
        "description" : "Gather what each store holds about one group into a single answer: the workspace graph's record of it and its members. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a group that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getGroupInfo",
        "parameters" : [ {
          "description" : "Identifier of the group to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the group"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a group",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/template-elements" : {
      "get" : {
        "description" : "Gather what each store holds about one template element into a single answer: the workspace graph's record of it and its path, the computed report and permissions, and the OpenSearch document. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a template element that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getTemplateElementInfo",
        "parameters" : [ {
          "description" : "Identifier of the template element to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the template element"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a template element",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/template-fields" : {
      "get" : {
        "description" : "Gather what each store holds about one template field into a single answer: the workspace graph's record of it and its path, the computed report and permissions, and the OpenSearch document. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a template field that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getTemplateFieldInfo",
        "parameters" : [ {
          "description" : "Identifier of the template field to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the template field"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a template field",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/template-instances" : {
      "get" : {
        "description" : "Gather what each store holds about one template instance into a single answer: the workspace graph's record of it and its path, the computed report and permissions, and the OpenSearch document. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a template instance that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getTemplateInstanceInfo",
        "parameters" : [ {
          "description" : "Identifier of the template instance to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the template instance"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a template instance",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/templates" : {
      "get" : {
        "description" : "Gather what each store holds about one template into a single answer: the workspace graph's record of it and its path, the computed report and permissions, and the OpenSearch document. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a template that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getTemplateInfo",
        "parameters" : [ {
          "description" : "Identifier of the template to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the template"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a template",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resource/users" : {
      "get" : {
        "description" : "Gather what each store holds about one user into a single answer: the workspace graph's record of it, the counts it can reach in each store, its group memberships, and the Keycloak account behind it. Written for diagnosis rather than for an application: the point is to see the stores side by side, since a user that behaves oddly usually has one store disagreeing with another. A store that cannot be reached leaves its section null rather than failing the request, and an identifier nothing knows returns an empty answer with 200.",
        "operationId" : "getUserInfo",
        "parameters" : [ {
          "description" : "Identifier of the user to report on.",
          "in" : "query",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "What each store holds about the user"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Get everything CEDAR knows about a user",
        "tags" : [ "Diagnostics" ]
      }
    },
    "/resources/counts" : {
      "get" : {
        "description" : "Report how many users, groups, categories, folders, and artifacts of each type Neo4j holds. This is the authoritative count; the search index is compared against it.",
        "operationId" : "resourceCounts",
        "responses" : {
          "200" : {
            "description" : "A count per kind, as the workspace graph has them"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Count what the workspace graph holds",
        "tags" : [ "Counts" ]
      }
    },
    "/resources/counts/opensearch" : {
      "get" : {
        "description" : "Report how many artifacts of each type OpenSearch has indexed. Read alongside the graph counts: the two disagreeing is how a half-finished reindex shows itself.",
        "operationId" : "openSearchCounts",
        "responses" : {
          "200" : {
            "description" : "A count per artifact type, as the search index has them"
          },
          "401" : {
            "description" : "Unauthorized"
          },
          "403" : {
            "description" : "The caller lacks the monitor read permission"
          },
          "500" : {
            "description" : "Internal server error"
          }
        },
        "security" : [ {
          "api_key" : [ ]
        } ],
        "summary" : "Count what the search index holds",
        "tags" : [ "Counts" ]
      }
    }
  },
  "components" : {
    "securitySchemes" : {
      "api_key" : {
        "description" : "Your CEDAR API key, using the format 'apiKey <yourApiKey>'",
        "in" : "header",
        "name" : "Authorization",
        "type" : "apiKey"
      }
    }
  }
}